// examples/demo.ltl — runnable demo for ltl_hkdf

import std::io
use ltl_hkdf as lib

fn main() {
    io::println("ltl_hkdf demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}